const accessToken = ''

const Spotify = {
    const getAccessToken = () => {
        if (accessToken) {
            return accessToken;
        } else {
            
        }
    }
};

export default Spotify;